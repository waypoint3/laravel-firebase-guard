<?php

use LeedsDigital\LaravelFirebaseGuard\Auth\FirebaseGuard;

beforeEach(function () {
    $this->guard = new FirebaseGuard();
});

test('checks user is authenticatable', function () {
    $this->instance
});
